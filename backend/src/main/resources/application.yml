spring:
  profiles:
    include:
      - server
  application:
    name: Ganhoho
  datasource:
    url: jdbc:mysql://localhost:3306/ganhoho
    username: d209
    password: d209
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  servlet:
    session:
      timeout: 2m

logging:
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n"

jwt:
  secret: lcCoZ5lvEmT7BNFMCz9TGgw0zy2lqTSv
  expiration:
    access: 86400000
    refresh: 604800000
